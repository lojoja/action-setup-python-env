[project]
name = "setup-python-env-poetry"
version = "0.0.0"
requires-python = ">=3.10,<4"
dependencies = [
  "platformdirs (==4.3.7)",
]

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=2.0.0"]

[tool.poetry]
package-mode = false
